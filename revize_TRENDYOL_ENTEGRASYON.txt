==============================================================
SMARTCART AI — TRENDYOL VERİ ENTEGRASYONU
Eski tüm ürünleri SİL, Trendyol verilerini yükle
==============================================================

MEVCUT SİSTEM:
- Backend: /Users/arif/Desktop/btk/smartcart-backend/
- Frontend: /Users/arif/Desktop/btk/e-commerce-prototype-2/
- Trendyol verileri: /Users/arif/Desktop/btk/trendyol_verileri/

TRENDYOL VERİ YAPISI (her ürün klasöründe):
- product_info.json → name, productImages[{originalUrl, localPath}]
- reviews.json → reviews[{userName, rating, comment, date, sellerName, images[{originalUrl}]}]
- summary.json → totalReviews, totalProductImages, totalReviewImages
- product_images/ → indirilen ürün görselleri
- review_images/ → indirilen yorum görselleri

9 ÜRÜN (ESKİ 8 ÜRÜNLERİ SİL, BUNLARI KULLAN):

ID=1: koton_pamuklu-normal-bel-skinny-fit-jean-pantolon-michael-jean_1019048304
  Ad: Koton Pamuklu Normal Bel Skinny Fit Jean Pantolon - Michael Jean
  Kategori: GIYIM, Marka: Koton, Yorum: 4, Ürün Görseli: 25, Yorum Görseli: 4

ID=2: koton_indigo-stone-erkek-jeans-5wam40120id_872446278
  Ad: Koton İndigo Stone Erkek Jeans 5WAM40120ID
  Kategori: GIYIM, Marka: Koton, Yorum: 33, Ürün Görseli: 27, Yorum Görseli: 33

ID=3: ikea_ahsap-27-5-cm-bambu-tabaklik-tabak-standi-ahsap-kahverengi-mutfak-1_27
  Ad: IKEA Ahşap 27,5 Cm Bambu Tabaklık - Tabak Standı
  Kategori: MOBILYA, Marka: IKEA, Yorum: 288, Ürün Görseli: 22, Yorum Görseli: 288

ID=4: kelebek_krem-gelin-battaniye-seti-fransiz-gupurludur-ceyizlik_148065462
  Ad: Kelebek Krem Gelin Battaniye Seti Fransız Güpürlüdür Çeyizlik
  Kategori: MOBILYA, Marka: Kelebek, Yorum: 477, Ürün Görseli: 32, Yorum Görseli: 477

ID=5: apple_pencil-usb-c-muwa3ze-a_779208863
  Ad: Apple Pencil (USB-C) MUWA3ZE/A
  Kategori: ELEKTRONIK, Marka: Apple, Yorum: 734, Ürün Görseli: 16, Yorum Görseli: 734

ID=6: samsung_toz-torbasiz-elektrikli-supurge-vc07r302mvp-tr_32662332
  Ad: Samsung Toz Torbasız Elektrikli Süpürge VC07R302MVP/TR
  Kategori: ELEKTRONIK, Marka: Samsung, Yorum: 848, Ürün Görseli: 16, Yorum Görseli: 848

ID=7: the-north-face_thermoball-unisex-siyah-bot_448153537
  Ad: The North Face Thermoball Unisex Siyah Bot
  Kategori: OUTDOOR, Marka: The North Face, Yorum: 53, Ürün Görseli: 29, Yorum Görseli: 53

ID=8: salomon_su-ve-soguga-karsi-dayanikli-erkek-kislik-outdoor-ayakkabisi_992682510
  Ad: Salomon Su ve Soğuğa Karşı Dayanıklı Erkek Kışlık Outdoor Ayakkabısı
  Kategori: OUTDOOR, Marka: Salomon, Yorum: 58, Ürün Görseli: 45, Yorum Görseli: 58

ID=9: burke_erkek-krem-renk-kaliteli-esnek-likrali-bilek-boy-kumas-pantolon-100-130-kg
  Ad: Bürke Erkek Krem Renk Kaliteli Esnek Likralı Bilek Boy Kumaş Pantolon
  Kategori: GIYIM, Marka: Bürke, Yorum: 1467, Ürün Görseli: 37, Yorum Görseli: 1467

==============================================================
ADIM 1 — Backend: TrendyolDataLoader oluştur
Dizin: /Users/arif/Desktop/btk/smartcart-backend/
==============================================================

Yeni dosya: src/main/java/com/smartcart/config/TrendyolDataLoader.java

Bu sınıf @Component + @PostConstruct ile çalışacak.
Trendyol JSON verilerini okuyup H2'ye yükleyecek.

YAPILACAKLAR:

1. trendyol_verileri/ dizinindeki her ürün klasörünü tara
2. product_info.json'dan ürün bilgilerini oku
3. reviews.json'dan yorumları oku
4. Veritabanına kaydet

DETAYLI TASARIM:

@Component
public class TrendyolDataLoader {

    @Value("${trendyol.data.path:/Users/arif/Desktop/btk/trendyol_verileri}")
    private String trendyolDataPath;

    @Autowired private ProductRepository productRepository;
    @Autowired private ReviewRepository reviewRepository;
    @Autowired private UserRepository userRepository;

    private final ObjectMapper mapper = new ObjectMapper();

    @PostConstruct
    @Transactional
    public void loadData() {
        if (productRepository.count() > 0) return; // zaten yüklenmiş

        File dataDir = new File(trendyolDataPath);
        if (!dataDir.exists()) {
            System.err.println("Trendyol veri dizini bulunamadı: " + trendyolDataPath);
            return;
        }

        // Kategoriler — klasör adından belirle
        Map<String, String> categoryMap = new HashMap<>();
        categoryMap.put("koton", "GIYIM");
        categoryMap.put("burke", "GIYIM");
        categoryMap.put("ikea", "MOBILYA");
        categoryMap.put("kelebek", "MOBILYA");
        categoryMap.put("apple", "ELEKTRONIK");
        categoryMap.put("samsung", "ELEKTRONIK");
        categoryMap.put("the-north-face", "OUTDOOR");
        categoryMap.put("salomon", "OUTDOOR");

        // Marka isimleri
        Map<String, String> brandMap = new HashMap<>();
        brandMap.put("koton", "Koton");
        brandMap.put("burke", "Bürke");
        brandMap.put("ikea", "IKEA");
        brandMap.put("kelebek", "Kelebek");
        brandMap.put("apple", "Apple");
        brandMap.put("samsung", "Samsung");
        brandMap.put("the-north-face", "The North Face");
        brandMap.put("salomon", "Salomon");

        File[] productDirs = dataDir.listFiles(File::isDirectory);
        if (productDirs == null) return;

        // Önce kullanıcı havuzu oluştur
        Set<String> createdEmails = new HashSet<>();
        List<User> allUsers = new ArrayList<>();

        int productId = 0;
        for (File productDir : productDirs) {
            productId++;
            String dirName = productDir.getName();
            String brandKey = dirName.split("_")[0]; // "koton", "ikea", vb.

            // 1. product_info.json oku
            File productInfoFile = new File(productDir, "product_info.json");
            if (!productInfoFile.exists()) continue;

            try {
                JsonNode productInfo = mapper.readTree(productInfoFile);
                String name = productInfo.get("name").asText();
                String brand = brandMap.getOrDefault(brandKey, brandKey);
                String category = categoryMap.getOrDefault(brandKey, "GIYIM");

                // Ürün görselleri — productImages dizisinden ilk 4 gerçek ürün fotoğrafını al
                // (svg, png ikonları, basket-icon gibi site UI öğelerini filtrele)
                List<String> imageUrls = new ArrayList<>();
                JsonNode images = productInfo.get("productImages");
                if (images != null && images.isArray()) {
                    for (JsonNode img : images) {
                        String url = img.get("originalUrl").asText();
                        // Sadece gerçek ürün fotoğraflarını al
                        // (svg, basket-icon, chevron, mastercard, app-store gibi UI öğelerini atla)
                        if (url.contains(".svg") || url.contains("basket") ||
                            url.contains("chevron") || url.contains("mastercard") ||
                            url.contains("americanexpress") || url.contains("visa") ||
                            url.contains("troy") || url.contains("app-store") ||
                            url.contains("appgallery") || url.contains("minimize") ||
                            url.contains("search_") || url.contains("sfint/prod/fp/")) {
                            continue;
                        }
                        imageUrls.add(url.trim());
                        if (imageUrls.size() >= 6) break; // max 6 görsel
                    }
                }
                String imageUrlStr = String.join(",", imageUrls);

                // Product oluştur ve kaydet
                Product product = new Product();
                product.setName(name);
                product.setBrand(brand);
                product.setCategory(category);
                product.setDescription(name + " - " + brand + " markasının kaliteli ürünü.");
                product.setImageUrl(imageUrlStr);
                product.setPrice(0.0); // Fiyat veri bulunamadı, placeholder
                product = productRepository.save(product);

                // 2. reviews.json oku
                File reviewsFile = new File(productDir, "reviews.json");
                if (reviewsFile.exists()) {
                    JsonNode reviewsData = mapper.readTree(reviewsFile);
                    JsonNode reviews = reviewsData.get("reviews");

                    if (reviews != null && reviews.isArray()) {
                        // Tekrarlayan reviewId'leri filtrele (aynı reviewId birden fazla olabilir)
                        Set<Long> seenReviewIds = new HashSet<>();
                        int reviewCount = 0;

                        for (JsonNode review : reviews) {
                            if (reviewCount >= 50) break; // max 50 yorum per ürün

                            long reviewId = review.get("reviewId").asLong();
                            if (seenReviewIds.contains(reviewId)) continue;
                            seenReviewIds.add(reviewId);

                            String userName = review.has("userName") ? review.get("userName").asText() : "Anonim";
                            int rating = review.has("rating") ? review.get("rating").asInt() : 5;
                            String comment = review.has("comment") ? review.get("comment").asText() : "";
                            if (comment.isBlank()) continue;

                            // Kullanıcı oluştur veya bul
                            String email = userName.replaceAll("[^a-zA-Z0-9]", "").toLowerCase() + reviewId + "@trendyol.com";
                            if (createdEmails.contains(email)) {
                                email = "u" + reviewCount + productId + "@trendyol.com";
                            }
                            User user;
                            if (!createdEmails.contains(email)) {
                                user = new User(null, userName, email);
                                user = userRepository.save(user);
                                allUsers.add(user);
                                createdEmails.add(email);
                            } else {
                                user = allUsers.get(allUsers.size() - 1);
                            }

                            // helpfulTags oluştur (yorum içeriğinden otomatik)
                            String helpfulTags = generateHelpfulTags(comment, rating);

                            // Yorum görseli URL'leri
                            List<String> reviewImageUrls = new ArrayList<>();
                            JsonNode imgs = review.get("images");
                            if (imgs != null && imgs.isArray()) {
                                for (JsonNode img : imgs) {
                                    if (img.has("originalUrl")) {
                                        reviewImageUrls.add(img.get("originalUrl").asText());
                                    }
                                }
                            }

                            Review rev = new Review();
                            rev.setUser(user);
                            rev.setProduct(product);
                            rev.setRating(rating);
                            rev.setComment(comment);
                            rev.setHelpfulTags(helpfulTags);
                            rev = reviewRepository.save(rev);

                            // ReviewImage kaydet (eğer entity varsa)
                            // ReviewImage entity yoksa bu kısmı atla
                            // Varsa: reviewImageRepository.save(new ReviewImage(null, rev, url, "Kullanıcı fotoğrafı"));

                            reviewCount++;
                        }
                        System.out.println("  ✅ " + product.getName() + " → " + reviewCount + " yorum yüklendi");
                    }
                }

            } catch (Exception e) {
                System.err.println("❌ Hata (" + dirName + "): " + e.getMessage());
            }
        }
        System.out.println("🎉 Trendyol verileri yüklendi! Toplam: " + productRepository.count() + " ürün");
    }

    private String generateHelpfulTags(String comment, int rating) {
        List<String> tags = new ArrayList<>();
        String lower = comment.toLowerCase();

        if (lower.contains("beden") || lower.contains("dar") || lower.contains("küçük")) tags.add("beden_sorunu");
        if (lower.contains("renk") || lower.contains("fark")) tags.add("renk_farkli");
        if (lower.contains("kalite") && rating <= 3) tags.add("kalite_kotu");
        if (lower.contains("kalite") && rating >= 4) tags.add("kalite_iyi");
        if (lower.contains("kargo") || lower.contains("hızlı")) tags.add("kargo_hizli");
        if (lower.contains("yırtık") || lower.contains("hasarlı") || lower.contains("bozuk")) tags.add("urun_hasarli");
        if (lower.contains("orijinal")) tags.add("orijinal");
        if (lower.contains("memnun") || lower.contains("güzel") || lower.contains("harika")) tags.add("memnun");
        if (lower.contains("iade") || lower.contains("iade ettim")) tags.add("iade");

        return String.join(",", tags);
    }
}

Import'lar:
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.smartcart.model.entity.*;
import com.smartcart.repository.*;
import jakarta.annotation.PostConstruct;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;
import org.springframework.transaction.annotation.Transactional;
import java.io.File;
import java.util.*;

─────────────────────────────────────────

ÖNEMLİ KURALLAR:

1. Mevcut data.sql'deki TÜM INSERT'leri SİL (products, product_specs, reviews).
   data.sql dosyası BOŞALTILACAK veya sadece tablo yapısı kalacak.
   Veriler artık TrendyolDataLoader'dan geliyor.

2. Mevcut SeedDataGenerator varsa DEVRE DIŞI BIRAK:
   @PostConstruct'un başına: if (true) return; ekle
   VEYA @Component annotation'ını kaldır
   VEYA sınıfı sil

3. application.properties'e ekle:
   trendyol.data.path=/Users/arif/Desktop/btk/trendyol_verileri

─────────────────────────────────────────

==============================================================
ADIM 2 — Frontend: Ürünleri API'den çek
Dizin: /Users/arif/Desktop/btk/e-commerce-prototype-2/app/page.tsx
==============================================================

Frontend'teki hardcoded ürün listesini (const products: Product[] = [...])
KALDIR. Yerine API'den çek.

Değişiklikler:

1. EcommercePage component'inde:

const [products, setProducts] = useState<Product[]>([])
const [loading, setLoading] = useState(true)

useEffect(() => {
  fetch(`${process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8080'}/api/products`)
    .then(r => r.json())
    .then(data => {
      const mapped = data.map((p: any) => ({
        id: p.id,
        category: (p.category || 'giyim').toLowerCase() as ProductCategory,
        brand: p.brand || '',
        title: p.name || p.title || '',
        price: p.price || 0,
        originalPrice: p.price ? p.price * 1.3 : 0,
        rating: p.reviews && p.reviews.length > 0
          ? p.reviews.reduce((sum: number, r: any) => sum + r.rating, 0) / p.reviews.length
          : 4.5,
        reviewCount: p.reviews ? p.reviews.length : 0,
        image: p.primaryImageUrl || (p.imageUrl ? p.imageUrl.split(',')[0].trim() : ''),
        imageUrls: p.imageUrls || (p.imageUrl ? p.imageUrl.split(',').map((u: string) => u.trim()) : []),
        sizes: p.category === 'GIYIM' || p.category === 'OUTDOOR' ? ['S','M','L','XL'] : undefined,
        colors: ['Varsayılan'],
        material: p.description || '',
        dimensions: '',
        seller: { name: p.brand || 'Satıcı', rating: 9.5 },
        reviews: (p.reviews || []).slice(0, 10).map((r: any, i: number) => ({
          id: r.id || i,
          author: r.username || r.userName || 'Anonim',
          rating: r.rating || 5,
          date: r.date || '',
          comment: r.comment || '',
          helpful: Math.floor(Math.random() * 100),
          images: r.images || []
        }))
      }))
      setProducts(mapped)
      setLoading(false)
    })
    .catch(err => {
      console.error('Ürün yükleme hatası:', err)
      setLoading(false)
    })
}, [])

2. Eski hardcoded const products: Product[] = [...] dizisini tamamen SİL (satır 42-210 civarı).

3. ProductCategory tipine "mobilya" | "elektronik" | "outdoor" | "giyim" zaten var.

4. categoryLabels'a uygun mapping:
   GIYIM → giyim, MOBILYA → mobilya, ELEKTRONIK → elektronik, OUTDOOR → outdoor

5. Loading durumu için:
   {loading ? (
     <div className="flex justify-center items-center min-h-screen">
       <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-primary" />
     </div>
   ) : (
     // mevcut sayfa içeriği
   )}

6. ProductCard görselini güncelle (hardcoded placeholder yerine):
   img src={product.image} kullan, onError'da fallback göster.

7. ProductDetailPage görsel galerisini güncelle:
   product.imageUrls dizisinden çoklu görsel göster.
   Thumbnail'lara tıklayınca büyük görsel değişsin.

==============================================================
ADIM 3 — mvn clean compile + test
==============================================================

Backend:
1. mvn clean compile → başarılı olmalı
2. mvn spring-boot:run → konsol'da:
   "✅ Koton Pamuklu... → X yorum yüklendi"
   "✅ Apple Pencil... → X yorum yüklendi"
   ...
   "🎉 Trendyol verileri yüklendi! Toplam: 9 ürün"

3. Test:
   curl http://localhost:8080/api/products
   → 9 ürün listesi (Trendyol ürünleri)

   curl http://localhost:8080/api/products/1
   → Koton jean + imageUrl (CDN URL'leri) + reviews (gerçek Trendyol yorumları)

Frontend:
1. pnpm dev (veya npm run dev)
2. localhost:3000 aç
   → Ürünler API'den yükleniyor (loading spinner → ürün kartları)
   → Kartlarda Trendyol CDN görselleri
   → Ürüne tıklayınca detay + gerçek yorumlar + yorum görselleri

==============================================================
